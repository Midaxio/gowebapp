server {
  listen   <%= node['nginx']['port'] -%>;
  server_name  <%= node['hostname'] %>;
  
  access_log  <%= node['nginx']['log_dir'] %>/goapp.access.log;

  location / {
    proxy_pass http://goapp;
  }
}
